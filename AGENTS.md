# AGENTS.md

Instructions for coding agents working on MoPai 墨排.

## Agent skills

### Issue tracker

Issues live as GitHub issues on `ye4wzp/mopai-markdown`, driven through the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` and one `docs/adr/` at the repo root, both created lazily. See `docs/agents/domain.md`.
