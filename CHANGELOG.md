# Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### Added

- 面板的空白状态（没有文档时）改成一张能用的卡片：品牌行 + 一句说明 + 真能点的动作。
  载入中是走马灯；载入失败写明原因并可「重试」；还没有文档时可以「新建空白文档」，
  也可以从「最近打开」里点开这个工作目录里已有的鱼排文档。
  宿主配套新增 `POST /fishpai/api/doc`（新建，`baseline` 保持 `null`）与
  `POST /fishpai/api/active`（切换当前文档）。
- 快捷键提示分系统显示：Mac 显示 `⌘` / `⌘⇧C`，其它平台显示 `Ctrl` / `Ctrl+Shift+C`。
  键盘处理一直同时接受 meta 与 ctrl，提示只是文案。
- 图标：鱼排自己的**鱼形标**（自绘，16px 网格、`currentColor`）出现在标签页 chip 与
  「新标签页」引导列表里（后者原先是没有 `icon` 时的立方体占位）。
- 主题列表换成带图标的列表：每套主题各有一个 16px 描边图标，取自 DSH 内建的图标集
  （`@deepseek-ai/dsh-client-ui-primitives`，右侧栏画图标用的同一套），不再用 emoji。
  原生 `<select>` 的 `<option>` 放不进 SVG，所以主题选择器改为自绘 listbox，
  保留 ↑↓ / Enter / Esc 与点击外部关闭。
  图标全部走 `currentColor` + `--dsw-alias-*` 令牌，**浅色与深色主题共用一套，不需要两套图**。
- **Markdown 编辑快捷键**：加粗 `Ctrl/⌘+B`、斜体 `+I`、行内代码 `+E`、链接 `+K`、
  删除线 `+Shift+X`、标题 `+Alt+1/2/3`（`+Alt+0` 去掉标题）、引用 `+Shift+.`、
  无序/有序列表 `+Shift+8` / `+Shift+7`；`Tab`/`Shift+Tab` 缩进与反缩进，列表/引用里回车自动接着写下一项
  （空条目再按一次退出）。编辑器头上的「快捷键」按钮给出与键盘处理**同源**的速查表。
  编辑动作走浏览器原生输入路径（`execCommand('insertText')`），所以 `Ctrl+Z` 撤销照旧可用。
- **图片直接粘进编辑器**：`Ctrl/⌘+V` 粘贴截图（或把图片文件拖进来）即存到**文档同级的 `assets/`**，
  正文自动插入 `![](assets/xxx.png)`；图片超过 5 MB 时当场说明而不是保存一张内嵌不了的图。
  宿主新增 `POST /fishpai/api/upload`（写图片资产的入口，文件名由宿主生成、只认栅格图扩展名）。
- 预览里的本地图片现在能看见：预览 iframe 是沙箱 `srcdoc`（不透明源），相对路径在它里面既取不到
  也会被同源守卫挡掉，所以由面板（同源）把图取回来转成 data URI 再塞进预览，按文档缓存、一张只取一次。

### Changed

- 主题清单与上游有意不同：移除「金融时报」（靠整页粉橙异色底立身，公众号文章不适合整页底色，
  去掉底后与相邻衬线主题几乎一样）与「Medium」（与「纽约时报」只剩字号与字体栈的差别）；
  「Claude」去掉整页米色底、其余风格保留。上游原版 `themes.js` 仍完整留在 `legacy-site/`。
  文档状态里若还存着被移除的主题，读取时退回「默认公众号」，不会让一篇文档打不开。

### Fixed

- **写入必须带 `base_revision`**（0.1.0 的守卫只在"带了版本"时才比对）：不传版本号或传 `null`
  过去等于强制覆盖——`Number(undefined)` 是 `NaN`，连"版本号非法"都识别不出来。
  现在路由层对缺失/非数字回 400，存储层把"拿不出合法版本号"与"版本不匹配"一并当冲突
  （409 + 回带服务端最新正文）。测试补齐了缺版本、非法类型、过期版本、并发同版本四类反例。
- **`PUT /doc` 缺 `markdown` 字段不再清空正文**（0.1.0 是 `String(body.markdown ?? '')`）：
  非字符串一律 400；要清空请显式传空字符串。
- **`GET /doc` 不再回带全量 `sessionId → docKey` 映射**：面板只要自己那一项，
  那份清单一直由 `GET /state` 提供（`docs` 同理）。
- **路径守卫拒绝 NTFS 备用数据流**：`notes.md:stream.txt` 的扩展名是 `.txt`、能过白名单，
  写进去的内容却挂在既有文件的隐藏流里（`readdir` 看不见）。Windows 上除盘符外 `:` 一律判非法。
- 「脚注」开关不再自锁：`linkCount` 原先在"脚注关着"时被记成 0，而面板用 `linkCount > 0`
  判断开关能不能点——关一次就置灰、再也打不开，提示还写着"正文里没有外链"。
  现在计数与开关无关，开关只决定要不要把它转成「参考资料」。
- 存图守卫移到**落盘之前**：`assets/` 若是指向工作目录之外的目录链接，原先会先写文件、
  再报"路径越界"（接口回 400，字节已经写到外面）。上传入口同时收窄到栅格图
  （`.svg` 仍可用于解析正文已引用的图，但不再能从上传进来），文件名不再叠出 `…-x.svg.png`；
  标题里的控制字符也不再进到文件路径里。
- 编辑器与面板：CRLF 文档里"在列表行中间按回车"不再失效（JS 的 `.` 不匹配 `\r`，
  带着它匹配行标记会整条失败）；剪贴板里同时有文字和图片时让文字优先（原先检测到图片就整条吞掉文本）；
  主题菜单的键盘处理移到列表容器上（Tab 进菜单后 ↑↓ 与 Esc 不再失灵，分组标题不再是 listbox 的非法子节点）；
  「回滚到某一版」先落盘未保存的输入（与「刷新」同一个约定），快速切换文档不再串台。
- 复制/导出补的不换行空格不再加到**块级**子元素前面：松散列表（条目间有空行）的首个子元素是 `<p>`，
  往它外面塞一个文字节点，反而会在微信里多出一个匿名行盒。
- **复制到公众号不再被平台的结构校验报「行高过小」**。公众号编辑器的 line-height 规则是实测的：
  `行数 = Range.getClientRects().length`，而**行内元素会把同一行拆成多个矩形**——一个
  `line-height: 1.8` 的两行段落只要含一个链接，矩形数就是 6，"平均行高"被算成 8.97px < 15.2px，
  于是被报成"行高小于字体大小，且存在多行文本，可能导致文字重叠（实测）"。
  现在复制/导出的产物会把文字包进 `<span>`（块级元素不再有直接文字子节点，那条规则不再命中），
  这也正是微信自己插入内容后的形态。渲染内核的默认路径保持与上游逐字节一致，
  兼容层只走 publish 一条路径、且是纯叠加（`test/wechat-structure.test.mjs` 守着）。
  `tech`/`gradient` 会命中 `darkmode-no-gradient`（与面板里"微信可能掉样式"的提示一致，
  要发公众号请用「默认公众号」）。**代码块不在这一层的覆盖范围内**：它的文字走 markdown-it 的
  `fence` 规则（mac 标题栏结构不能动），而代码块天然多行，同一套算法照样算小——带围栏代码块的文章
  仍会被标出这条 line-height（上游形态也一样）。
- **引用块的框、表头底色在公众号里不再消失**：微信编辑器的安全过滤按**属性名**过，
  `background-color` 在名单里而 `background` 简写不在——主题里写的 `background: #f0f8f0`
  粘过去会被整条丢掉（实测：竹林主题的引用块在面板里是绿框，粘进公众号成了普通段落）。
  复制/导出现在把简写拆成 `background-color:` / `background-image:`（渐变走后者），
  是纯规范化，视觉与浏览器里完全一致。
- **列表项粘进公众号不再被拆成两行**：微信会把条目**开头**的行内片段单独起一行，
  于是 `**批注**：光标停在哪一段…` 变成"批注"一行、`：光标…`一行（面板预览里是正常的一行）。
  两轮对照样张在真实公众号里量过边界：条目以文字开头、加粗在中段、以及段落里的加粗开头都正常；
  "整条 li 包一个 span"试过无效（已撤）。现在复制/导出会在"以行内元素开头"的条目前面补一个
  不换行空格（U+00A0）——一个文字节点即可解决，视觉上约 1/4 字缩进，且结构校验会把它当空白。
- 包裹层不再把首尾空白包进 `<span>`（微信会把"以空白开头的行内元素"当成新的行/块），也不再产生 `<span></span>` 空壳。
- 面板变窄时「导出 HTML / 复制到公众号」不再被挤到第二行：三个动作按钮包成一组（整组换行），
  窄栏下收紧内边距，它们在 400 多像素的面板里仍待在右上角。

## [0.1.0] - 2026-09-15

首个版本。fork 自 [mopai-markdown](https://github.com/ye4wzp/mopai-markdown)（MIT），重构为 DSH 插件。

### Added

- 宿主侧工具 `fishpai_open` / `fishpai_read` / `fishpai_write` / `fishpai_render`：
  打开文档、读块级 diff 与批注、按 `base_revision` 局部改稿、导出可粘贴的 HTML。
  `path` / `out_path` 可省略扩展名（自动补 `.md` / `.html`）。
- 宿主侧 `/fishpai/api/*` 路由与文档存储（`.fishpai/` 下的 `docs/` 文档、`state/` 状态与 `history/` 快照），路径限定在会话工作目录内。
- 客户端右侧栏面板：Markdown 源码 + 公众号实时预览、13 主题与 12 预设色、字号、
  微信脚注与 mac 代码块开关、手机 375px 预览、复制到公众号、导出 HTML。
  - 面板显示的信息（块清单、行内占位、图片与「需手动上传」提示、批注锚点、历史）跟着当前正文实时刷新。
  - 批注可一键定位并选中它引用的片段；被改写过的引用会退回原块并说明原因。
  - 复制/导出失败给出原因与替代按钮；批注写入失败时已输入的文字保留在输入框里。
- 块级批注与行内占位（`<!-- 鱼排: … -->`），供模型读取人改了什么、想要什么。
- `fishpai` 技能：主题选择、按块改稿纪律、排版建议。
- 渲染内核 ESM 化，并固化为字节级 golden 回归测试。
