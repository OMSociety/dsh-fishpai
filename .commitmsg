feat(editor): Markdown 快捷键、粘贴图片，并按实测收敛主题

编辑侧终于像个编辑器了：
- 新增 client/mdedit.ts：加粗/斜体/行内代码/链接/删除线/标题/引用/列表的快捷键，
  Tab 缩进、Shift+Tab 反缩进、回车续列表；SHORTCUTS 一张表同时喂键盘匹配、
  速查表与测试，不存在"改了键、提示没改"。编辑动作走 execCommand('insertText')，
  Ctrl+Z 的原生撤销栈照旧可用。
- 图片可以直接粘进来（或拖进来）：POST /fishpai/api/upload 把它存到文档同级的
  assets/，正文插入 ![](assets/xxx.png)；预览由面板同源取回图片、转 data URI 内联
  （预览 iframe 是不透明源，相对路径在里面既取不到也会被同源守卫挡掉）。

按微信实测收敛主题与样式：
- 移除「金融时报」（整页粉橙异色底，公众号文章不要整页底色，去掉底后与相邻衬线主题
  几乎一样）与「Medium」（与「纽约时报」只剩字号与字体栈的差别）；「Claude」去掉
  整页米色底。上游原版 themes.js 仍在 legacy-site/ 里，golden 用默认 oracle 重生成。
- 新增第二层微信兼容（wechatBackground，只走复制/导出）：把 background 简写拆成
  background-color / background-image。微信的安全过滤按属性名过，简写会被整条丢掉，
  实测竹林主题的引用块粘进公众号后框和底色全没了。纯规范化，视觉一致。
- 状态里存着已移除的主题时退回默认，不让一篇文档因为一个主题名打不开。

面板细节：
- 窄栏下「导出 HTML / 复制到公众号」不再被挤到第二行（三个动作按钮包成一组 + 收紧内边距）。
- 预览能显示本地图片了。

微信官方校验器实测（正文段落/引用/列表/表格/信息卡片）：11 套主题里「适合公众号」的
8 套 + dark_night 全部通过，tech/gradient 只命中 darkmode-no-gradient。代码块是已知边界：
它的文字走 fence 规则、wrapText 包不到，带围栏的文章仍会被标出 line-height（上游亦然）。

测试 154 → 170 项（mdedit 单测、上传路由与边界、底色兼容层的纯规范化性质、已删主题回退）。
